Run from CMD:
java -jar "LanguageProcessor.jar" "test file name"

The files generated by the parser and the intermediate code 
are found inside the test folder.
The Object Code generated is found inside the Object Code folder,
inside the test folder. 

If you want to run your own tests, you can create a txt and put it inside
in the TDL Tests folder along with the rest of the tests.